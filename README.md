# Impressive Data Site

Requirements: Node 18+ recommended.

Install:
npm insteall
npm run dev

Drop/update `public/data.csv` (already present). The app auto-parses CSV and runs the analysis module in `src/utils/userProgram.js`.

Deploy: push to GitHub and use Vercel/Netlify.
